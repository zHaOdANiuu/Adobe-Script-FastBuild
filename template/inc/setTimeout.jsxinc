var setTimeout =
  app.version > 14
    ? function (func, delay) {
      return app.setTimeout(func, delay || 0);
    }
    : function (func, delay) {
      return app.scheduleTask(uneval(func) + '()', delay || 0, false);
    };

export default setTimeout;
